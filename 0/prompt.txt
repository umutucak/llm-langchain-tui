We have a disconnect between two tables now. Even though all conversations are saved, only the ones that are manually saved can be resumed. I want to know hook up auto-titling after first llm response. User can then rename via the /name command if they want. I want to achieve this with tool calling. Similar to a system prompt, how can I give an instruction to the model to use this tool call wuthout exposing it to the user experience?